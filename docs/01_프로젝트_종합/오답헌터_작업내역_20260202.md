# 오답헌터 작업 내역 (2026-02-02)

## 1. 계정 초기화 기능 추가
- **설정 > 데이터 탭**에 계정 초기화 버튼 추가
- `Database.js`에 `clear()` 메서드 추가 (IDB store 초기화)
- `Game.js`에 `resetAccount()` 메서드 구현 (런/아이템 초기화, 레벨1/골드100 복원, 몬스터 유지)
- 4개 언어 번역 추가 (ko/en/ja/zh)

## 2. 로그인 화면 문구 변경
- `i18n.js`의 `htmlSlogan` 키: "오답을 잡아 레벨업하자!" → "내 오답이 몬스터가 된다!"

## 3. 로그인 화면 그리드 배경 + 캔버스 네온 효과
- `style.css` `#login-screen`에 CSS 그리드 패턴 배경 추가
- `Renderer.js` 그리드 색상 강화: 메인 `rgba(120,130,255,0.5)` + `shadowBlur: 3` 네온 효과

## 4. UI 색상 조정
- **AI 문제 생성 버튼**: 반투명 → 불투명 `#1d4ed8` (파란색), 테두리 `#4b8df8`
- **상점 버튼 테두리**: `#22c55e` (초록색)
- **통계 버튼 테두리**: `#38bdf8` (하늘색)
- **통계 화면 카드 테두리**: 전체 `#38bdf8`로 통일
- **업적 달성 카드**: 금색 테두리 `#fbbf24` 추가
- **소비 아이템 구매 버튼**: 노란색 (`COLORS.WARNING`) + 검정 텍스트

## 5. 업적 복수 해금 큐 시스템
- `EffectSystem.js`에 `_achievementQueue` 배열 추가
- 배너 표시 중 새 업적 해금 시 큐에 저장, 순차 표시

## 6. 크로스플랫폼 버그 수정

### CRITICAL (4건)
| 항목 | 수정 내용 |
|------|----------|
| iOS Safe Area | `viewport-fit=cover`, CSS `env(safe-area-inset-*)` 패딩, 로그아웃 버튼 오프셋 |
| Android 뒤로가기 | `popstate` 이벤트 핸들러로 메인 화면 복귀 |
| Android 키보드 오버레이 | `showPrompt()`에 `visualViewport` resize 리스너 |
| iOS input.focus() | `setTimeout` 지연 + `scrollIntoView` 처리 |

### HIGH (3건)
| 항목 | 수정 내용 |
|------|----------|
| aggregateStats() catch 누락 | `.catch()` 추가 |
| 더블클릭 방지 | `handleInput()`에 200ms 쿨다운 |
| nextMonster() 레이스 컨디션 | `_loadingMonster` 가드 플래그 |

### MEDIUM (1건)
| 항목 | 수정 내용 |
|------|----------|
| localStorage try-catch | `utils/storage.js` 안전 래퍼 생성 (`safeGetItem`/`safeSetItem`/`safeRemoveItem`), 8개 파일 약 30개 호출 전부 교체. 시크릿/프라이빗 브라우징 모드 에러 방지 |

## 7. 설정 화면 탭 분류
- 기존 단일 스크롤 → 3개 탭 구조로 리팩토링
- **게임** (🎮): 사운드, 언어, UI 투명도
- **API** (🔑): SmilePrint, Gemini, API 발급 안내, AI 기능 설명
- **데이터** (📦): 내보내기/가져오기, 문제 초기화, 가이드 다시보기, 계정 초기화
- 4개 언어 탭 라벨 번역 추가

## 수정 파일 목록
- `app/index.html` - viewport-fit, 구조
- `app/src/style.css` - safe area, 그리드 배경
- `app/src/main.js` - Android 뒤로가기, 드래그, localStorage 래퍼
- `app/src/game/Game.js` - 계정초기화, 더블클릭방지, 레이스컨디션, 키보드, localStorage 래퍼
- `app/src/game/EffectSystem.js` - 업적 큐
- `app/src/game/screens/SettingsScreen.js` - 탭 분류
- `app/src/game/screens/MainScreen.js` - 버튼 색상
- `app/src/game/screens/ShopScreen.js` - 구매버튼 색상
- `app/src/game/screens/StatsScreen.js` - 카드 테두리
- `app/src/game/screens/AchievementScreen.js` - 금색 테두리
- `app/src/canvas/Renderer.js` - 네온 그리드, UI 투명도, localStorage 래퍼
- `app/src/i18n/i18n.js` - 문구 변경, 탭 번역, 계정초기화 번역, localStorage 래퍼
- `app/src/utils/storage.js` - 신규 (안전 localStorage 래퍼)
- `app/src/services/GeminiService.js` - localStorage 래퍼
- `app/src/services/ImageAnalysisService.js` - localStorage 래퍼
- `app/src/services/ProblemGeneratorService.js` - localStorage 래퍼
- `app/src/services/SoundService.js` - localStorage 래퍼
- `app/src/data/Database.js` - clear() 메서드
