# 랜덤배경2 / HQ모드 전체 삭제 완료

## 작업 일자
2026-02-09

## 작업 내용
randomBg2, randomBg3, HQ모드 관련 기능을 전부 삭제

## 수정된 파일 (11개)

### 1. CLAUDE.md
- "절대 수정 금지 항목" 섹션 전체 삭제

### 2. app/src/utils/constants.js
- `randomBg2`, `randomBg3` 상점 아이템 정의 삭제

### 3. app/src/game/ShopManager.js
- `randomBg2`, `randomBg3` 구매 로직 블록 삭제
- 미사용 `safeGetItem` import 제거

### 4. app/src/game/screens/ShopScreen.js
- 숨겨진 영역 전체 삭제 (randomBg2 카드 + randomBg3 카드)
- 미사용 `safeGetItem` import 제거

### 5. app/src/game/screens/SettingsScreen.js
- HQ모드 토글 전체 삭제 (해금 전/후 분기 모두)
- 미사용 `safeGetItem`, `safeSetItem` import 제거

### 6. app/src/canvas/Renderer.js
- init()에서 `_mx1`, `_px1` 초기화 제거
- `_loadSpecialBgList()`, `setRandomBgImage()`, `_loadAltBgList()`, `setAltBgImage()` 함수 전체 삭제

### 7. app/src/game/Game.js
- import에서 `sha256` 제거
- `_promptGuideCode1()`, `_promptGuideCode2()` 함수 삭제
- 가이드 화면에서 코드 입력 버튼 (촬영/공격) 삭제
- `promptApiKey()`에서 sha256 해시 비교 블록 삭제
- `_doAIGenerateMenu()`에서 특수 입력 체크 블록 삭제
- `resetAccount()`에서 `_ux1`, `_mx1`, `_bx1` 제거 코드 삭제

### 8. app/src/main.js
- 특수 모드 초기화 코드 삭제 (`_ux1`, `_mx1`, `_bx1` safeRemoveItem)

### 9. app/src/i18n/i18n.js
- 4개 언어 모두에서 삭제: `randomBg2`, `randomBg2Desc`, `randomBg3`, `randomBg3Desc`, `_m1`, `enterCode`, `invalidCode`, `hqPerfDesc`

### 10. app/vite.config.js
- `syncAltRes()` 함수 삭제
- `generateAll()`에서 `syncAltRes()` 호출과 res/a1, res/b1 list 생성 제거
- configureServer에서 proport 감시 제거

### 11. app/src/utils/storage.js
- `sha256()` 함수 삭제

## 삭제된 파일/폴더
- `app/public/res/a1/` 폴더 전체
- `app/public/res/b1/` 폴더 전체
- `app/public/res/` 폴더 (비어있으므로 삭제)
- `docs/HQ모드_세션초기화_20260203.md`
- `docs/특수배경모드_구현완료_20260203.md`
- `weather/access.md`
- `weather/weather_report_보고서.md`

## 빌드 검증
- `npm run build` 성공 (628ms, 에러 없음)
